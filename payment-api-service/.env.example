# Copy to .env and fill in. Read via spring.config.import in application.yml.
DB_URL=jdbc:postgresql://localhost:5432/payledger
DB_USERNAME=postgres
DB_PASSWORD=
# Base64-encoded key for HS256. Generate one with:
#   openssl rand -base64 32
JWT_SECRET=
